coordinates = (3, 5)
print(coordinates[0])  # Output: 3

# The main difference between tuples and lists in Python is that tuples are immutable, while lists are mutable.

# This means that once you create a tuple, you cannot change its contents. 